######securityContext was added in record 67
#[2023-02-08T02:29:52.630Z] FAIL - k8s_deployment_service.yaml - main - Containers must not run as root - use runAsNonRoot wihin container security context
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: demo-svc
  name: demo-pod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-svc
  strategy: {}
  template:
    metadata:
      labels:
        app: demo-svc
    spec:
      #serviceAccountName: default
      containers:
      - name: demo-con
        image: eagunuworld/demo-gamesnake:BUILD_TAG
        ports:
        - containerPort: 3000
      #  securityContext:
        #  runAsNonRoot: true
        #  runAsUser: 100
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: demo-svc
  name: demo-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
    nodePort: 30000
  selector:
    app: demo-svc
  type: NodePort
